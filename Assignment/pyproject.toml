[project]
name = "assignment"
version = "0.1.0"
description = "An interactive library system for the Python basics assignment"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
