[project]
name = "ml-project-oms"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "[aiagentohms]", email = "[mangostick112@gmail.com]" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
ml-project-oms = "ml_project_oms:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
